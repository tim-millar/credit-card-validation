#!/usr/bin/env ruby

require 'bundler/setup'
require 'credit_card_validation'

card_number = ARGV[0]

puts 'Card type is: ' + CreditCardValidation::CardType.validate(card_number)
puts 'Card number is: ' + CreditCardValidation::CardNumber.validate(card_number)
